/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'plus.unsplash.com','instagram.fraj2-1.fna.fbcdn.net']
    }
};

export default nextConfig;
